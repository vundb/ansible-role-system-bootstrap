#
# Perform update for system package tree.
#
system_update: yes


#
# Array with host entries.
#
# system_host_entries:
#   - { ip: "127.0.0.1", name: "localhost", state: "present" }
#   - { ip: "::1",       name: "localhost", state: "absent" }
#
system_host_entries: []
